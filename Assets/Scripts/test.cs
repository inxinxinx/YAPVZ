using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    /*
    GameObject cur;
    public void shoot()
    {
        for(int i = 0; i < 10; i++)
        {
            i = i / 3;
            switch (i)
            {
                case 0:
                    cur = new peashooter() as GameObject;
                    break;
                case 1:
                    break;
                case 2:
                    break;
                case 3:
                    break;
                default:
                     break;
            }
        }
    }
    */

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
